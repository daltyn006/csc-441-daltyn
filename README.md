This is a file you should read.
markdown is a language that allows for better visualization than normal text.
it allows for image embedding, stylization, font, text size, and other methodology.
This is why README's are generally set in markdown format.
